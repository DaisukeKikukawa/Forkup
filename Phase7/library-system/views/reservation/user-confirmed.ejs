<!DOCTYPE html>
<html lang="ja">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>library-system</title>
  <link rel="stylesheet" href="/reservation/user-confirmed.css">
</head>

<body>
  <div class="container">
    <div class="user-info">
      <h3>予約申込者情報</h3>
      <div class="user-detail">
        <strong>利用者ID:</strong>
        <%= user.id %>
      </div>
      <div class="user-detail">
        <strong>氏名:</strong>
        <%= user.name %>
      </div>
      <div class="user-detail">
        <strong>メール:</strong>
        <%= user.email %>
      </div>
      <% if (user.phone) { %>
        <div class="user-detail">
          <strong>電話番号:</strong>
          <%= user.phone %>
        </div>
        <% } %>
    </div>

    <div class="reservation-status">
      <div class="reservation-count">
        現在の予約数: <%= reservationCount %> / <%= maxReservations %>件
      </div>
    </div>

    <div class="current-reservations">
      <h3>現在の予約一覧 (<%= reservationCount %>件)</h3>

      <% if (activeReservations && activeReservations.length> 0) { %>
        <ul class="reservation-list">
          <% activeReservations.forEach(reservation=> { %>
            <li class="reservation-item">
              <div>
                <div class="book-title">
                  <%= reservation.book ? reservation.book.title : '不明な書籍' %>
                </div>
              </div>
            </li>
            <% }) %>
        </ul>
        <% } else { %>
          <div class="no-reservations">
            現在予約中の書籍はありません
          </div>
          <% } %>
    </div>

    <div class="next-step">
      <a href="/reservation/book-input?userId=<%= user.id %>" class="btn">書籍選択に進む</a>
      <a href="/reservation/start" class="btn btn-secondary">キャンセル</a>
    </div>
  </div>
</body>

</html>
