<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <h1>Posts</h1>
  <ul>
    <% posts.forEach((post,index) => { %>
      <li>
        <h2>
          <a href="/posts/<%= index %>"><%= post.title %></a>
          <a href="/posts/<%= index %>/edit">編集</a>
          <form method="post" action="/posts/<%= index %>?_method=DELETE">
            <input type="hidden" name="id" value="<%= index %>">
            <input type="submit" value="削除">
          </form>
        </h2>
        <p>
          <%= post.body %>
        </p>
      </li>
      <% }) %>
  </ul>
  <p><a href="/posts/new">新規作成</a></p>
</body>
</html>
